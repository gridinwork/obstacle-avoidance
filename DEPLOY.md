# 🚀 Быстрая выгрузка на GitHub

## ✅ Что уже готово:
- ✅ Git репозиторий инициализирован
- ✅ Все файлы добавлены и закоммичены
- ✅ README обновлен с полным описанием проекта
- ✅ Версия 74 указана

## 📋 Способы выгрузки на GitHub:

### Способ 1: Через веб-интерфейс GitHub (САМЫЙ ПРОСТОЙ) ⭐

1. **Откройте**: https://github.com/new

2. **Заполните форму:**
   - **Repository name**: `obstacle-avoidance-v74` или `программа-объезда-препятствий`
   - **Description**: `Программа для объезда препятствий - версия 74. LiDAR A1 + Raspberry Pi 4 + Pixhawk`
   - **Visibility**: Public (или Private)
   - ⚠️ **НЕ** ставьте галочки на "Add a README file", ".gitignore", "license" (они уже есть!)

3. **Нажмите**: "Create repository"

4. **Выполните команды** (GitHub покажет их после создания):
   ```powershell
   cd D:\Cursor\Projects\version_74
   git remote add origin https://github.com/ВАШ_USERNAME/obstacle-avoidance-v74.git
   git branch -M main
   git push -u origin main
   ```

### Способ 2: Через Cursor UI

1. Нажмите `Ctrl+Shift+G` (Source Control)
2. Нажмите "..." (три точки) вверху
3. Выберите "Publish Branch"
4. Войдите в GitHub (если нужно)
5. Создайте новый репозиторий или выберите существующий
6. Нажмите "Publish"

### Способ 3: Через скрипт с GitHub API

**Требуется Personal Access Token:**

1. Создайте токен: https://github.com/settings/tokens
   - Нажмите "Generate new token (classic)"
   - Выберите scope: `repo` (полный доступ к репозиториям)
   - Скопируйте токен

2. Запустите скрипт:
   ```powershell
   cd D:\Cursor\Projects\version_74
   .\create_and_push_github.ps1 -GitHubToken "ваш-токен" -Username "ваш-username"
   ```

### Способ 4: Через GitHub CLI (если установлен)

```powershell
cd D:\Cursor\Projects\version_74
gh auth login
gh repo create obstacle-avoidance-v74 --public --description "Программа для объезда препятствий - версия 74" --source=. --remote=origin --push
```

## 🔐 Авторизация при push

Если GitHub запросит авторизацию:
- **Username**: ваш GitHub username
- **Password**: используйте **Personal Access Token** (не пароль!)

Как создать токен:
1. https://github.com/settings/tokens
2. Generate new token (classic)
3. Scope: `repo`
4. Скопируйте и используйте как пароль

## ✨ После успешной выгрузки

Проект будет доступен по адресу:
**https://github.com/ваш-username/obstacle-avoidance-v74**

## 📝 Информация о проекте

- **Название**: Программа для объезда препятствий
- **Версия**: 74
- **Оборудование**: 
  - LiDAR A1
  - Raspberry Pi 4 (4GB)
  - Pixhawk 2/4/8
  - Гусеничное шасси с двумя моторами
- **Опционально**: Камера для визуального мониторинга
