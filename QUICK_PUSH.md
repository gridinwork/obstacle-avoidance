# Быстрая выгрузка на GitHub

## ✅ Что уже сделано:
- ✅ Git репозиторий инициализирован
- ✅ Все файлы добавлены
- ✅ Создан первый коммит

## 📋 Что нужно сделать:

### Вариант 1: Через веб-интерфейс GitHub (рекомендуется)

1. **Создайте новый репозиторий на GitHub:**
   - Откройте https://github.com/new
   - Введите название репозитория (например: `version_74` или `obstacle-mission-v74`)
   - **НЕ** добавляйте README, .gitignore или лицензию (они уже есть)
   - Нажмите "Create repository"

2. **Скопируйте URL репозитория:**
   - После создания GitHub покажет URL, например: `https://github.com/ваш-username/version_74.git`

3. **Выполните команды в терминале Cursor:**
   ```powershell
   cd D:\Cursor\Projects\version_74
   git remote add origin https://github.com/ваш-username/version_74.git
   git branch -M main
   git push -u origin main
   ```

   Или используйте готовый скрипт:
   ```powershell
   .\push_to_github.ps1 -RepoUrl "https://github.com/ваш-username/version_74.git"
   ```

### Вариант 2: Через Cursor UI

1. **Откройте Source Control панель:**
   - Нажмите `Ctrl+Shift+G`

2. **Опубликуйте ветку:**
   - Нажмите на кнопку "..." (три точки) вверху панели
   - Выберите "Publish Branch" или "Push"
   - Если появится запрос на авторизацию - войдите в GitHub
   - Создайте новый репозиторий или выберите существующий

### Вариант 3: Через GitHub CLI (если установлен)

```powershell
cd D:\Cursor\Projects\version_74
gh auth login
gh repo create version_74 --public --source=. --remote=origin --push
```

## 🔐 Если требуется авторизация:

При первом push GitHub может запросить:
- **Username** - ваш GitHub username
- **Password** - используйте Personal Access Token (не пароль!)

Как создать токен:
1. https://github.com/settings/tokens
2. Generate new token (classic)
3. Выберите scope: `repo`
4. Скопируйте токен и используйте его как пароль

## ✨ После успешной выгрузки:

Проект будет доступен по адресу:
`https://github.com/ваш-username/название-репозитория`
